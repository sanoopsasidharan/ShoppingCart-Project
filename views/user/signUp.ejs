<style>
  /* BASIC */

  html {
    background-color: #56baed;
  }

  body {
    font-family: "Poppins", sans-serif;
    height: 100vh;
  }

  a {
    color: #92badd;
    display: inline-block;
    text-decoration: none;
    font-weight: 400;
  }

  h2 {
    text-align: center;
    font-size: 16px;
    font-weight: 600;
    text-transform: uppercase;
    display: inline-block;
    margin: 40px 8px 10px 8px;
    color: #cccccc;
  }



  /* STRUCTURE */

  .wrapper {
    display: flex;
    align-items: center;
    flex-direction: column;
    justify-content: center;
    width: 100%;
    min-height: 100%;
    padding: 20px;
  }

  #formContent {
    -webkit-border-radius: 10px 10px 10px 10px;
    border-radius: 10px 10px 10px 10px;
    background: #fff;
    padding: 30px;
    width: 90%;
    max-width: 450px;
    position: relative;
    padding: 0px;
    -webkit-box-shadow: 0 30px 60px 0 rgba(0, 0, 0, 0.3);
    box-shadow: 0 30px 60px 0 rgba(0, 0, 0, 0.3);
    text-align: center;
  }

  #formFooter {
    background-color: #f6f6f6;
    border-top: 1px solid #dce8f1;
    padding: 25px;
    text-align: center;
    -webkit-border-radius: 0 0 10px 10px;
    border-radius: 0 0 10px 10px;
  }



  /* TABS */

  h2.inactive {
    color: #cccccc;
  }

  h2.active {
    color: #0d0d0d;
    border-bottom: 2px solid #5fbae9;
  }



  /* FORM TYPOGRAPHY*/

  input[type=button],
  input[type=submit],
  input[type=reset] {
    background-color: #56baed;
    border: none;
    color: white;
    padding: 15px 80px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    text-transform: uppercase;
    font-size: 13px;
    -webkit-box-shadow: 0 10px 30px 0 rgba(95, 186, 233, 0.4);
    box-shadow: 0 10px 30px 0 rgba(95, 186, 233, 0.4);
    -webkit-border-radius: 5px 5px 5px 5px;
    border-radius: 5px 5px 5px 5px;
    margin: 5px 20px 40px 20px;
    -webkit-transition: all 0.3s ease-in-out;
    -moz-transition: all 0.3s ease-in-out;
    -ms-transition: all 0.3s ease-in-out;
    -o-transition: all 0.3s ease-in-out;
    transition: all 0.3s ease-in-out;
  }

  input[type=button]:hover,
  input[type=submit]:hover,
  input[type=reset]:hover {
    background-color: #39ace7;
  }

  input[type=button]:active,
  input[type=submit]:active,
  input[type=reset]:active {
    -moz-transform: scale(0.95);
    -webkit-transform: scale(0.95);
    -o-transform: scale(0.95);
    -ms-transform: scale(0.95);
    transform: scale(0.95);
  }

  input[type=text] {
    background-color: #f6f6f6;
    border: none;
    color: #0d0d0d;
    padding: 15px 32px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 5px;
    width: 85%;
    border: 2px solid #f6f6f6;
    -webkit-transition: all 0.5s ease-in-out;
    -moz-transition: all 0.5s ease-in-out;
    -ms-transition: all 0.5s ease-in-out;
    -o-transition: all 0.5s ease-in-out;
    transition: all 0.5s ease-in-out;
    -webkit-border-radius: 5px 5px 5px 5px;
    border-radius: 5px 5px 5px 5px;
  }

  input[id=password] {
    background-color: #f6f6f6;
    border: none;
    color: #0d0d0d;
    padding: 15px 32px;
    text-align: center;
    text-decoration: none;
    display: inline-block;
    font-size: 16px;
    margin: 5px;
    width: 85%;
    border: 2px solid #f6f6f6;
    -webkit-transition: all 0.5s ease-in-out;
    -moz-transition: all 0.5s ease-in-out;
    -ms-transition: all 0.5s ease-in-out;
    -o-transition: all 0.5s ease-in-out;
    transition: all 0.5s ease-in-out;
    -webkit-border-radius: 5px 5px 5px 5px;
    border-radius: 5px 5px 5px 5px;
  }


  input[type=text]:focus {
    background-color: #fff;
    border-bottom: 2px solid #5fbae9;
  }

  input[id=password]:focus {
    background-color: #fff;
    border-bottom: 2px solid #5fbae9;
  }

  input[type=text]:placeholder {
    color: #cccccc;
  }

  input[id=password]:placeholder {
    color: #dbd2d2;
  }




  /* ANIMATIONS */

  /* Simple CSS3 Fade-in-down Animation */
  .fadeInDown {
    -webkit-animation-name: fadeInDown;
    animation-name: fadeInDown;
    -webkit-animation-duration: 1s;
    animation-duration: 1s;
    -webkit-animation-fill-mode: both;
    animation-fill-mode: both;
  }

  @-webkit-keyframes fadeInDown {
    0% {
      opacity: 0;
      -webkit-transform: translate3d(0, -100%, 0);
      transform: translate3d(0, -100%, 0);
    }

    100% {
      opacity: 1;
      -webkit-transform: none;
      transform: none;
    }
  }

  @keyframes fadeInDown {
    0% {
      opacity: 0;
      -webkit-transform: translate3d(0, -100%, 0);
      transform: translate3d(0, -100%, 0);
    }

    100% {
      opacity: 1;
      -webkit-transform: none;
      transform: none;
    }
  }

  /* Simple CSS3 Fade-in Animation */
  @-webkit-keyframes fadeIn {
    from {
      opacity: 0;
    }

    to {
      opacity: 1;
    }
  }

  @-moz-keyframes fadeIn {
    from {
      opacity: 0;
    }

    to {
      opacity: 1;
    }
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }

    to {
      opacity: 1;
    }
  }

  .fadeIn {
    opacity: 0;
    -webkit-animation: fadeIn ease-in 1;
    -moz-animation: fadeIn ease-in 1;
    animation: fadeIn ease-in 1;

    -webkit-animation-fill-mode: forwards;
    -moz-animation-fill-mode: forwards;
    animation-fill-mode: forwards;

    -webkit-animation-duration: 1s;
    -moz-animation-duration: 1s;
    animation-duration: 1s;
  }

  .fadeIn.first {
    -webkit-animation-delay: 0.4s;
    -moz-animation-delay: 0.4s;
    animation-delay: 0.4s;
  }

  .fadeIn.second {
    -webkit-animation-delay: 0.6s;
    -moz-animation-delay: 0.6s;
    animation-delay: 0.6s;
  }

  .fadeIn.third {
    -webkit-animation-delay: 0.8s;
    -moz-animation-delay: 0.8s;
    animation-delay: 0.8s;
  }

  .fadeIn.fourth {
    -webkit-animation-delay: 1s;
    -moz-animation-delay: 1s;
    animation-delay: 1s;
  }

  /* Simple CSS3 Fade-in Animation */
  .underlineHover:after {
    display: block;
    left: 0;
    bottom: -10px;
    width: 0;
    height: 2px;
    background-color: #56baed;
    content: "";
    transition: width 0.2s;
  }

  .underlineHover:hover {
    color: #0d0d0d;
  }

  .underlineHover:hover:after {
    width: 100%;
  }

  .h1Tag {
    margin-bottom: 25px;
    margin-top: 35px;
  }



  /* OTHERS */

  *:focus {
    outline: none;
  }

  #icon {
    width: 60%;
  }
</style>

<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<link href="//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
<script src="//maxcdn.bootstrapcdn.com/bootstrap/4.0.0/js/bootstrap.min.js"></script>
<script src="//cdnjs.cloudflare.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
<!------ Include the above in your HEAD tag ---------->
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery-validate/1.19.3/jquery.validate.js"></script>

<!-- Modal -->
<div class="modal fade" id="exampleModal" tabindex="-1" role="dialog" aria-labelledby="exampleModalLabel"
  aria-hidden="true">
  <div class="modal-dialog" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
      <div class="modal-body">
        ...
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
        <button type="button" class="btn btn-primary">Save changes</button>
      </div>
    </div>
  </div>
</div>

<div class="wrapper fadeInDown">
  <div id="formContent">
    <!-- Tabs Titles -->

    <!-- Icon -->
    <!-- <div class="fadeIn first">
          <img src="http://danielzawadzki.com/codepen/01/icon.svg" id="icon" alt="User Icon" />
        </div> -->

    <!-- Login Form -->




    <form id="siginup_form">
      <h1 class="h1Tag">SignUp</h1>

      <input type="text" id="login" class="fadeIn second" name="name" placeholder="Name" maxlength="15" />
      <div class="invalid-feedback" id="name-Text">enter your Name</div>

      <input type="text" id="mailValidation" class="fadeIn third" name="email" placeholder="Email">
      <div class="invalid-feedback" id="mail-type"> Enter email address </div>

      <input type="text" id="numberValidation" class="fadeIn third" name="number" placeholder="Phone" maxlength="10" />
      <div class="invalid-feedback" id="text-Number">Enter your phone number </div>

      <input type="password" id="password" class="fadeIn third" name="password" placeholder="Password" maxlength="15">
      <div class="invalid-feedback" id="password-Text">password</div>

      <input type="text" name="usedRefferalCode" id="referralCodeOrginal" hidden>

      <div style="display: flex; justify-content: center;">
        <button style="padding: 15px; color: rgb(0,123,255);" onclick="showReffrealform()" type="button"
          id="show-reffreal-form">Referral</button>
        <button style="padding: 15px; color: red;" onclick="hideReffrealform()" type="button"
          id="hide-reffreal-form">Cancel</button>
      </div>


      <a style=" margin-top: 10 px; margin-bottom: 15px;" href="/login">you have already an account !</a>

      <input style="margin: 0; padding: 10px;" type="submit" id="login" class="fadeIn fourth" value="SignUp">


      <p style="color:red;" id="erorrMessage"></p>


    </form>
    <div class="mt-4" id="reffeal_form_div">
      <form id="refferal_from">
        <input type="text" name="code" placeholder="Referral code" id="">
        <p class="mt-3" id="coupone-error-message"></p>
        <!-- <div class="invalid-feedback" id="refferal-error">refferal</div> -->
        <button
          style="background-color: rgb(86,186,237);color: rgb(255, 255, 255); margin-top: 10px; margin-bottom: 15px;"
          class="btn" type="submit">check</button>
      </form>
    </div>

    <div>

      <form id="otpform">
        <div style="display: flex; justify-content: center;">
          <h1 class="m-5">OTP validation</h1>
        </div>
        <div style="display: flex; justify-content: space-around;">
          <p id="timer"></p>
          <p style="color: red; cursor: pointer;" onclick="retryOTP()">Retry</p>
        </div>
        <input type="text" name="number" placeholder="Number" id="Otpnumber">
        <input type="text" name="code" placeholder="Otp" id="">

        <input style="margin: 0; padding: 10px;" type="submit" class="fadeIn fourth" value="Sent">
      </form>
    </div>



  </div>
</div>
<script src="https://code.jquery.com/jquery-3.6.0.js" integrity="sha256-H+K7U5CnXl1h5ywQfKtSj8PCmoN9aaq30gDh27Xc0jk="
  crossorigin="anonymous"></script>
<script src="//cdn.jsdelivr.net/npm/sweetalert2@11"></script>
<!-- <script src="https://cdn.jsdelivr.net/npm/jquery-validation@1.19.3/dist/jquery.validate.js"></script> -->

<script>

  let timerOn = true;

  function timer(remaining) {
    var m = Math.floor(remaining / 60);
    var s = remaining % 60;

    m = m < 10 ? '0' + m : m;
    s = s < 10 ? '0' + s : s;
    document.getElementById('timer').innerHTML = m + ':' + s;
    remaining -= 1;

    if (remaining >= 0 && timerOn) {
      setTimeout(function () {
        timer(remaining);
      }, 1000);
      return;
    }

    if (!timerOn) {
      // Do validate stuff here
      return;
    }

    // Do timeout stuff here
    Swal.fire({
      title: 'Timeout for otp',
      text: "You won't resent OTP !",
      icon: 'warning',
      showCancelButton: true,
      confirmButtonColor: '#3085d6',
      cancelButtonColor: '#d33',
      confirmButtonText: 'Yes'
    }).then((result) => {
      if (result.isConfirmed) {
         $("#otpform").load(location.href + " #otpform");
        location.reload()
      }
    })
  }

  timer(120);
</script>


<script>

  document.getElementById('otpform').style.display = 'none'

  function sentOtps() {
    var phoneNumber = parseInt(document.getElementById('numberValidation').value)

    if ($("#numberValidation").val().length === 10) {

      document.getElementById('Otpnumber').value = phoneNumber

      $.ajax({
        url: '/registationOtpchecking',
        data: { number: phoneNumber },
        method: 'post',
        success: (response) => {
          if (response.status) {
            document.getElementById('siginup_form').style.display = 'none'
            document.getElementById('otpform').style.display = 'block'
          }
        }

      })
    }

  }

  function retryOTP() {
    
   
    var phoneNumber = parseInt(document.getElementById('numberValidation').value)

    if ($("#numberValidation").val().length === 10) {

      document.getElementById('Otpnumber').value = phoneNumber

      $.ajax({
        url: '/registationOtpchecking',
        data: { number: phoneNumber },
        method: 'post',
        success: (response) => {
          if (response.status) {
            document.getElementById('siginup_form').style.display = 'none'
            document.getElementById('otpform').style.display = 'block'
          }
        }

      })
    }



  }


  $('#otpform').submit((sub) => {
    sub.preventDefault()
    $.ajax({
      url: '/registration-sucess-otp',
      data: $('#otpform').serialize(),
      method: 'post',
      success: (response) => {
        if (response.status) {
          document.getElementById('siginup_form').style.display = 'block'
          document.getElementById('otpform').style.display = 'none'
        } else {
          Swal.fire(
            'error !',
            'Otp not match',
            'warning'
          )

          document.getElementById('siginup_form').style.display = 'none'
          document.getElementById('otpform').style.display = 'block'
        }
      }
    })
  })



  // refferal coupon 
  document.getElementById('reffeal_form_div').style.display = 'none'
  document.getElementById('hide-reffreal-form').style.display = 'none'

  function showReffrealform() {
    document.getElementById('reffeal_form_div').style.display = 'block'
    document.getElementById('show-reffreal-form').style.display = 'none'
    document.getElementById('hide-reffreal-form').style.display = 'block'
  }

  function hideReffrealform() {
    document.getElementById('reffeal_form_div').style.display = 'none'
    document.getElementById('show-reffreal-form').style.display = 'block'
    document.getElementById('hide-reffreal-form').style.display = 'none'
  }
  // refferal coupon end
  $('#refferal_from').submit((e) => {
    e.preventDefault();
    $.ajax({
      url: '/checking-refferal',
      data: $('#refferal_from').serialize(),
      method: 'post',
      success: (response) => {
        if (response.status) {
          document.getElementById('referralCodeOrginal').value = response.CpId
          document.getElementById('coupone-error-message').style.color = 'green'
          document.getElementById('coupone-error-message').innerHTML = 'applied referral code'
        } else {
          document.getElementById('referralCodeOrginal').value = ''
          document.getElementById('coupone-error-message').style.color = 'red'
          document.getElementById('coupone-error-message').innerHTML = 'sorry invalid referral code'
        }
      }
    })
  })

</script>
<script>
  var nameValidated = false;
  var phoneValidated = false;
  var passwordValidated = false;
  var emailValidated = false;


  $("#login").keyup(function () {
    if ($("#login").val().match(/^[A-Za-z][A-Za-z\ ]/)) {
      $("#name-Text").show();
      $("#name-Text").text(" ");
      nameValidated = true;
    } else {
      $("#name-Text").show();
      $("#name-Text").text("Invalid name! try again");
      $("#name-Text").css("class", "invalid-feedback");
      nameValidated = false;
    }
    $("#login").keypress(function (num) {
      if ((num.charCode >= 33 && num.charCode <= 64) || (num.charCode >= 91 && num.charCode <= 96) || (num.charCode >= 123 && num.charCode <= 126)) {
        num.preventDefault();
      }
    });
    if ($("#login").val().length < 3) {
      $("#name-Text").show();
      $("#name-Text").text("Name should have type morethan 3 characters");
      $("#name-Text").css("class", "invalid-feedback");
      nameValidated = false;
    }
  });
  $("#login").blur(function () {
    if ($("#login").val() == "") {
      $("#name-Text").show();
      $("#name-Text").text("Name should not be empty!");
      $("#name-Text").css("class", "invalid-feedback");
      nameValidated = false;
    }
  });


  $("#password").keyup(function () {
    if ($("#password").val().match(/^[A-Za-z][A-Za-z\ ]/)) {
      $("#password-Text").show();
      $("#password-Text").text(" ");
      passwordValidated = true;
    } else {
      $("#password-Text").show();
      $("#password-Text").text("Invalid password! try again");
      $("#password-Text").css("class", "invalid-feedback");
      passwordValidated = false;
    }
    $("#password").keypress(function (num) {
      if ((num.charCode >= 33 && num.charCode <= 64) || (num.charCode >= 91 && num.charCode <= 96) || (num.charCode >= 123 && num.charCode <= 126)) {
        num.preventDefault();
      }
    });
    if ($("#password").val().length < 3) {
      $("#password-Text").show();
      $("#password-Text").text("password should have type morethan 3 characters");
      $("#password-Text").css("class", "invalid-feedback");
      passwordValidated = false;
    }
  });
  $("#password").blur(function () {
    if ($("#password").val() == "") {
      $("#password-Text").show();
      $("#password-Text").text("password should not be empty!");
      $("#password-Text").css("class", "invalid-feedback");
      passwordValidated = false;
    }
  });
  $("#mailValidation").keyup(function () {
    if ($("#mailValidation").val().match(/^[^\s@]+@[^\s@]+\.[^\s@]+$/)) {
      $("#mail-type").text(" ");
      $("#mail-type").show();
      emailValidated = true;
    } else {
      $("#mail-type").show();
      $("#mail-type").text("Invalid email address");
      $("#mail-type").css("class", "invalid-feedback");
      emailValidated = false;
    }
  });
  $("#mailValidation").blur(function () {
    if ($("#mailValidation").val() == "") {
      $("#mail-type").show();
      $("#mail-type").text("Must enter email");
      $("#mail-type").css("class", "invalid-feedback");
      emailValidated = false;
    }
  });


  $("#numberValidation").keypress(function (num) {
    return num.charCode >= 48 && num.charCode <= 57;
  });
  $("#numberValidation").keyup(function () {
    if ($("#numberValidation").val().match(/^\d+$/)) {
      $("#text-Number").show();
      $("#text-Number").text(" ");
      phoneValidated = true;
    }
    if ($("#numberValidation").val().length < 10) {
      $("#text-Number").show();
      $("#text-Number").text("Phone number should be 10 digits!");
      $("#text-Number").css("class", "invalid-feedback");
      phoneValidated = false;
    }
  });

  $("#numberValidation").blur(function () {
    if ($("#numberValidation").val() == "") {
      $("#text-Number").show();
      $("#text-Number").text("Phone number is required!");
      $("#text-Number").css("class", "invalid-feedback");
      phoneValidated = false;
    } else {
      sentOtps();
    }
  });



  $("#siginup_form").submit(function (num) {
    if ($("#login").val() == "" || $("#password").val() == "" || $("#mailValidation").val() == "" || $("#numberValidation").val() == "") {
      // alert("please fill the form properly");
      Swal.fire(
        'Unknown error !',
        'please fill the form properly',
        'warning'
      )
    }
    if ($("#login").val() == "" && $("#password").val() == "" && $("#mailValidation").val() == "" && $("#numberValidation").val() == "") {
      num.preventDefault();
      $("#name-Text").show();
      $("#mail-type").show();
      $("#text-Number").show();
      $("#password-Text").show();
    } else if (
      nameValidated &&
      passwordValidated &&
      emailValidated &&
      phoneValidated
    ) {
      num.preventDefault();
      $.ajax({
        url: '/signup',
        data: $('#siginup_form').serialize(),
        method: 'post',
        success: (response) => {
          if (response.status) {
            location.href = '/login'
          } else {
            document.getElementById('erorrMessage').innerHTML = "sorry enter another number"
          }
        }
      })

    } else {
      num.preventDefault();
    }
  });



  // $('#siginup_form').submit(function (num) {
  //   num.preventDefault();
  //   $.ajax({
  //     url: '/signup',
  //     data: $('#siginup_form').serialize(),
  //     method: 'post',
  //     success: (response) => {
  //       alert(response.status)
  //       if (response.status) {
  //         location.href = '/login'
  //       } else {
  //         document.getElementById('erorrMessage').innerHTML = "sorry change your number"
  //       }
  //     }
  //   })
  // })


</script>